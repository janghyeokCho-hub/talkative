import { Route } from "react-router-dom";
import React, { lazy, Suspense } from "react";

// const AsyncHome = lazy(() => import("@/components/AppTemplate"));
export { default as Main } from "@/containers/main/MainContainer";
